
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Campaign Analytics Report</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; background-color: #f4f4f4; margin: 0; padding: 20px; }
        .container { max-width: 800px; margin: 20px auto; background: #fff; padding: 30px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); }
        h1 { color: #0056b3; text-align: center; margin-bottom: 30px; }
        .section-title { font-size: 1.2em; color: #0056b3; border-bottom: 1px solid #eee; padding-bottom: 10px; margin-top: 20px; margin-bottom: 15px; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 20px; text-align: center; }
        .stat-item { background-color: #e9f7ff; padding: 15px; border-radius: 5px; }
        .stat-value { font-size: 1.8em; font-weight: bold; color: #007bff; }
        .stat-label { font-size: 0.9em; color: #555; }
        .email-content-preview { border: 1px solid #ddd; padding: 15px; background-color: #f9f9f9; border-radius: 5px; margin-top: 20px; }
        .email-content-preview h3 { margin-top: 0; color: #0056b3; }
        .email-content-preview pre { white-space: pre-wrap; word-wrap: break-word; }
        .footer { text-align: center; margin-top: 30px; font-size: 0.8em; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Email Campaign Analytics Report</h1>

        <div class="section-title">Campaign Summary</div>
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-value"><%= totalEmails %></div>
                <div class="stat-label">Total Emails Attempted</div>
            </div>
            <div class="stat-item">
                <div class="stat-value" style="color: #28a745;"><%= successfulEmails %></div>
                <div class="stat-label">Successfully Sent</div>
            </div>
            <div class="stat-item">
                <div class="stat-value" style="color: #dc3545;"><%= failedEmails %></div>
                <div class="stat-label">Failed to Send</div>
            </div>
        </div>

        <div class="section-title">Campaign Details</div>
        <p><strong>Subject:</strong> <%= subject %></p>
        <p><strong>Email Type:</strong> <%= emailType %></p>

        <div class="section-title">Original Email Content</div>
        <div class="email-content-preview">
            <% if (emailType === 'html') { %>
                <h3>HTML Content:</h3>
                <div style="border: 1px solid #eee; padding: 10px; background-color: #fff;"><%- originalMessage %></div>
            <% } else { %>
                <h3>Plain Text Content:</h3>
                <pre><%= originalMessage %></pre>
            <% } %>
        </div>

        <div class="footer">
            Report generated by G-Realm Studio Email Sender
        </div>
    </div>
</body>
</html>
